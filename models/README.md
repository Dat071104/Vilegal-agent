# Models

This directory is intended for storing trained model weights, checkpoints, and LoRA adapters.

**Note:** Model artifacts are large and should NOT be committed to git. They are ignored in `.gitignore`.
