# Data Directory

This directory stores the datasets used by the ViLegal Agent.

**Data Handling Policy:**
- Raw legal data (`data/raw/`) and processed datasets (`data/processed/`) are **NOT** committed to git.
- Only this README and `.gitkeep` files are tracked.
- Any scripts to download or generate data should be placed in `scripts/` or `notebooks/`.
